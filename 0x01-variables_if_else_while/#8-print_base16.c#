#include <stdio.h>
/**
 * main - Prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch = '1'
char a = 'a'
  
for (ch = '1'; ch <= '9'; ch++)
  for (a = '1'; a <= 
putchar(ch);

  return (0);
}
